# This file makes the WOGO pipeline a Python package.

# It is the main application for the WOGO API.

__version__ = "0.1.0"

# It must exist even if empty with just comments so Python can safely import from this folder without screaming "module not found".